export const SET_STATE_TIMECARDS = (state, {timecards}) => {
    state.timecards = timecards;
}

export const SET_STATE_USERS = (state, { users }) => {
    state.users = users;
}

export const SET_STATE_PROJECTS = (state, { projects }) => {
    state.projects = projects;
}

export const SET_STATE_ROLES = (state, { roles }) => {
    state.roles = roles;
}